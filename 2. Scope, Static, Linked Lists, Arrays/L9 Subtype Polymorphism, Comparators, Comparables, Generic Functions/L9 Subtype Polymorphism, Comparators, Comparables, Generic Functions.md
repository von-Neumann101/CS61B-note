多态：同一个函数可以对不同对象作用，得到类似的结果（根据不同的对象）

使用接口，可以使得某个类具有某种属性，甚至是必须的（如果你有compareTo但是没有实现Comparable接口，也无法调用对应方法）

**Comparator vs Comparable**
![[Pasted image 20260324200458.png|501]]


假设我们现在做一个随机在数组中取数的方法，我们现在要他拓展到所有类型的数组
这样（使用泛型），但是不会工作，因为pickRandom是静态方法（类的泛型参数属于“对象维度”，而 `static` 方法属于“类维度”，两者作用域不相交）
![[Pasted image 20260324201010.png]]
但是我们又不想实例化这个对象
**可以这么做**：
![[Pasted image 20260324201200.png]]
我正在声明一个在类型T对象上工作的公共静态函数，这就可以正常工作而且不用实例化
Java不会自己猜，所以下面的代码是错误的
![[Pasted image 20260324202012.png]]
但是我们可以告诉Java传入的一定是Comparable的
![[Pasted image 20260324201955.png]]