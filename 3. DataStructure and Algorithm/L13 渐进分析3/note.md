需要对增长敏感，如果翻倍或者+1，每个阶具体会如何
![[Pasted image 20260326210105.png]]
$$\sum_{i=0}^Nf(i)\in\Theta(\int_0^Nf(x)dx)$$

![[Pasted image 20260327082719.png|412]]
首先看外层，i是指数增长的，循环次数是lg N(但是要注意，**这里不能内部循环次数乘以外部循环次数，因为内部循环和外部循环直接关联**)
对于每一个i，我们每次执行的次数是：1 2 4 8 ...... N
内层循环就是$\sum_{i=1,2,4,...}^Ni$是指数增长，令i=2^t ，直接得到$\sum_{t=1}^{\lg N}2^t$求和，其阶为
2^lgN=N

我们以双向数组的resize为例
对于每次+FACTOR，我们可以直接简化FACTOR=1，因为这并不影响阶，等于说我们加n个数，每加一个数，就要有一个阶为O(n)的操作，所以总的是O(n^2)
再来看看每次xFACTOR，我们加n个数，m x 2^k=n（m是扩大前的数，k是扩大次数），我们扩大lg(n)次，每次操作都是O(2^k)次，总操作次数就是2+4+8+...+2^lg(n)次，就是2^lgn次就是n次

我们还可以通过“摄动法：解决
![[Pasted image 20260327093611.png]]
比如这道题，如果我们计算n+1，就需要把n的计算量翻倍，所以是2^N

其实对于斐波那契数列，也是一样的，只是我们递推的树稍微会小一点


**归并排序**
![[Pasted image 20260327160135.png]]
每次把一个数组分为两份，每一份都排序，我们只需要“相邻比较”即可
![[Pasted image 20260327160533.png|451]]
对于两个排序好的数组，归并排序的时间复杂度为$O(n)$
这是一个递归过程，所以我们考虑总的时间复杂度
```
		     16
	  8             8
  4       4       4       4
2   2   2   2   2   2   2   2
```
N个数，分N/2和N/2，我们先把N/2和N/2排序好，我们只需要考虑N/2就行了
所以我们进行k=lg N次分割
![[Pasted image 20260327162257.png|468]]
说一下第三个等号，因为C(1) = 1，系数是2^lgN,所以说是N
